#include <cstdlib>
#include <iomanip>
#include <iostream>


int main(int argc, char const *argv[]) {
  // TradingAccount trading_acc;
  // std::cout << "Account Equity: " << std::setprecision(10)
  //           << trading_acc.getEquity() << std::endl;

  // std::string order_symbol = "SNAP";
  // int order_qty = 1;
  // std::string order_side = "buy";
  // std::string order_type = "market";
  // std::string time_in_force = "day";

  // std::cout << "\n"
  //           << "Sending trade" << std::endl;
  // trading_acc.submitMarketOrder(order_symbol, order_qty, order_side, order_type,
  //                               time_in_force);

  return 0;
}